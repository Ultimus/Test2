package TowerAlpha;

public class Drache extends Enemy {

static double speed = 1.3;

public Drache(int xPar, int yPar){
	
	super(xPar, yPar, speed);
		this.hp = 30; // Beispiel jede Jank hat 10 HP muss nicht im Konstrukter uebergeben werden Change 13.05
		this.direction = 1;
		this.width = 90;
		this.heigth = 60;
		this.bounty = 100;
		this.type = 3;
		//this.type = (int)(1+Math.random()*2); // definiert aussehen floss oder schiff
}
	
public Drache(int xPar, int yPar, int additionalHP){
		
		super(xPar, yPar, speed);
			this.hp = 30+additionalHP; // Beispiel jede Jank hat 10 HP muss nicht im Konstrukter uebergeben werden Change 13.05
			this.direction = 1;
			this.width = 90;
			this.heigth = 60;
			this.bounty = 100;
			this.type = 3;
			//this.type = (int)(1+Math.random()*2); // definiert aussehen floss oder schiff
	}
}
